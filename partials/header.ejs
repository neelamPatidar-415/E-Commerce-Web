<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <!-- <div class="flex place-content-between px-2">
        <h3 class="text-lg font-semibold px-0 py-0">Scatch</h3>
        <% var isloggedin = (typeof(loggedin) !== "undefined")? loggedin:true %>
        <% if(isloggedin){ %>
            <div class="flex gap-5">
                <a href="">Shop</a>
                <a href="">Cart</a>
                <a href="">My Account</a>
                <a href="/users/logout"  class="text-red-600">logout</a>
            </div>
        <% }%>
    </div> -->

    <div class="fixed top-0 left-0 w-full z-50 bg-white/80 backdrop-blur-md shadow-md flex justify-between px-4 py-3">
  <h3 class="text-lg font-semibold">Scatch</h3>
  <% var isloggedin = (typeof(loggedin) !== "undefined") ? loggedin : true %>
  <% if(isloggedin) { %>
    <div class="flex gap-5">
      <a href="">Shop</a>
      <a href="">Cart</a>
      <a href="">My Account</a>
      <a href="/users/logout" class="text-red-600">logout</a>
    </div>
  <% } %>
</div>

</body>
</html>